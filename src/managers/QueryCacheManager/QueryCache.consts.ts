export const DEFAULT_PAGINATION_PATHS = {
  totalElementsPath: 'data.page.totalElements',
  totalPagesPath: 'data.page.totalPages',
  currentPagePath: 'data.page.number',
  pageSizePath: 'data.page.size',
} as const;
